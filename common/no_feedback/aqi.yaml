# Configuration for not showing AQI status

switch:
  - platform: template
    id: show_bad
    optimistic: true
  - platform: template
    id: show_acceptable
    optimistic: true
  - platform: template
    id: show_good
    optimistic: true
